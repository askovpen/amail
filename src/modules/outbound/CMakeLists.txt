if (ENABLE_BSO)
    add_subdirectory(bso)
endif()
if (ENABLE_ASO)
    add_subdirectory(aso)
endif()
if (ENABLE_FBOX)
    add_subdirectory(fbox)
endif()
if (ENABLE_LBOX)
    add_subdirectory(lbox)
endif()
